import 'package:clean_code/src/parser.dart';
class CCGenerator {
  /// the main function that generates directories and
  /// files
  void generate(List<Item> jobs) {
    for (final job in jobs) {
      //TODO
    }
  }
}
